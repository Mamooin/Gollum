def area(a, h):
    """ѕолучает переменные a и h, возвращает площ€дь треугольника со стороной a и высатой h"""
    return a * h / 2


def perimeter(a, b, c):
    """ѕолучает переменные a, b и c, возвращает длинну периметра треугольника со сторонами a, b и с"""
    return a + b + c
