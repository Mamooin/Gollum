def area(a, b):
    """ѕолучает переменные a и b, возвращает площ€дь пр€моугольника со сторонами a и b"""
    return a * b


def perimeter(a, b):
    """ѕолучает переменные a и b, возвращает длинну периметра со сторонами a и b"""
    return 2*(a + b)
